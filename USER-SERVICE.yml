server:
    port: 8090

spring:
    datasource:
        url: jdbc:mysql://172.31.21.128:8251/microservices?useSSL=false
        username: root
        password: Vasmaster@20!5
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true

eureka:
    instance:
        prefer-ip-address: true
        hostname: localhost
    client:
        register-with-eureka: true
        fetch-registry: true
        service-url:
            defaultZone: http://localhost:8761/eureka/
